# Node.js dependencies
node_modules/
.pnp/
.pnp.js

# build output
build/
dist/
out/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
.yarn-integrity

# environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local # Catches any other local .env files

# local development server
.parcel-cache/
.vite/ # For Vite projects

# editor and IDE files
.idea/ # IntelliJ IDEA
.vscode/ # VS Code settings (optional, sometimes you might want to share specific settings)
*.swp
*.swo
.DS_Store # macOS specific
Thumbs.db # Windows specific

# dependency lock files (optional, depends on preference/setup)
# If you use yarn.lock or package-lock.json for strict dependency control, you might want to commit them.
# If you manage dependencies manually or don't need strict locking in your repo, you can ignore them.
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# temporary files
*.tmp
*.bak

# test coverage
coverage/

# Vercel specific files (if deploying to Vercel)
.vercel/

# Netlify specific files (if deploying to Netlify)
.netlify/

# Storybook specific files (if using Storybook)
.storybook/out/

# Cypress specific files (if using Cypress for E2E tests)
cypress/videos/
cypress/screenshots/

# ESLint cache
.eslintcache

# TypeScript build artifacts (if not part of the main build folder)
*.tsbuildinfo

# Miscellaneous
*.log
.DS_Store